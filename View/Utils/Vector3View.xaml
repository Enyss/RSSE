<UserControl x:Class="RSSE.Vector3View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RSSE"
             mc:Ignorable="d" 
             d:DesignHeight="28" d:DesignWidth="300">
    <DockPanel>
        <Grid Width="55"  DockPanel.Dock="Right">
            <Label Content="z" HorizontalAlignment="Left" />
            <TextBox Text="{Binding Path=z}" Width="40" HorizontalAlignment="Right" Height="22" TabIndex="3"/>
        </Grid>
        <Grid Width="55"  DockPanel.Dock="Right">
            <Label Content="y" HorizontalAlignment="Left" />
            <TextBox Text="{Binding Path=y}" Width="40" HorizontalAlignment="Right" Height="22" TabIndex="2"/>
        </Grid>
        <Grid Width="55"  DockPanel.Dock="Right" HorizontalAlignment="Right">
            <Label Content="x" HorizontalAlignment="Left" />
            <TextBox Text="{Binding Path=x}" Width="40" HorizontalAlignment="Right" Height="22" TabIndex="1"/>
        </Grid>
    </DockPanel>
</UserControl>
